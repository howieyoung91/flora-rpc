flora:
  rpc:
    server:
      port: 2001
      serializer: KRYO
      compressor: NOCOMPRESS
      application:
        namespace: /flora/service/flora
        # group:
        # version:
      registry:
        zookeeper:
          # address: 
          base-sleep-time: 3000
          max-retries: 3
        # nacos:
